using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Background : MonoBehaviour
{
    //FIX THIS LATER
    
    
    /*
    [SerializeField] private Material material;
    [SerializeField] private string materialTagPlayerPos = "_PlayerPos";
    private EntitySpawner es;
    private GameObject player;
    private void Start()
    {
        es = EntitySpawner.Instance;
        es.OnSpawnPlayer += PlayerSpawn;
    }

    private void Update()
    {
        if (player == null) return;
        
        material.SetVector(materialTagPlayerPos, player.transform.position);
    }

    private void OnDisable()
    {
        es.OnSpawnPlayer -= PlayerSpawn;
    }

    private void PlayerSpawn(object sender, GameObject p)
    {
        player = p;
    }
    */
}
